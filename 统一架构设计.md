# 电商系统统一架构设计

## 1. API设计规范

### RESTful API设计原则
- **资源命名**: 使用复数名词，如 `/api/v1/products`
- **HTTP方法**:
  - `GET`: 获取资源
  - `POST`: 创建资源  
  - `PUT`: 更新资源（全量）
  - `PATCH`: 更新资源（部分）
  - `DELETE`: 删除资源
- **版本控制**: URL路径版本 `/api/v1/`
- **分页参数**: `page`, `pageSize`, `total`
- **排序参数**: `sortBy`, `sortOrder`

### 统一响应格式
```typescript
// 成功响应
{
  "code": 200,
  "message": "success",
  "data": T, // 实际数据
  "timestamp": 1678888888
}

// 错误响应
{
  "code": 400,
  "message": "参数错误",
  "errors": [
    {
      "field": "username",
      "message": "用户名不能为空"
    }
  ],
  "timestamp": 1678888888
}
```

### HTTP状态码映射
- `200`: 成功
- `201`: 创建成功
- `400`: 客户端错误（参数验证失败）
- `401`: 未认证
- `403`: 无权限
- `404`: 资源不存在
- `409`: 资源冲突
- `429`: 请求过多
- `500`: 服务器错误

## 2. 数据模型设计

### 核心实体关系
```
User ──┬── UserProfile
       ├── UserAddress
       ├── ShoppingCart ── CartItem ── Product
       └── Order ── OrderItem ── Product
            └── Payment
```

### TypeScript类型定义
```typescript
// 基础类型
interface BaseEntity {
  id: string;
  createdAt: string;
  updatedAt: string;
}

// 用户相关
interface User extends BaseEntity {
  username: string;
  email: string;
  phone?: string;
  avatarUrl?: string;
  status: 'active' | 'inactive' | 'banned';
  isEmailVerified: boolean;
  isPhoneVerified: boolean;
  lastLoginAt?: string;
}

interface UserProfile extends BaseEntity {
  userId: string;
  realName?: string;
  gender?: 'male' | 'female' | 'other';
  birthDate?: string;
  idCardNumber?: string;
}

interface UserAddress extends BaseEntity {
  userId: string;
  recipientName: string;
  phone: string;
  province: string;
  city: string;
  district: string;
  streetAddress: string;
  postalCode?: string;
  isDefault: boolean;
}

// 商品相关
interface Category extends BaseEntity {
  parentId?: string;
  name: string;
  slug: string;
  description?: string;
  imageUrl?: string;
  sortOrder: number;
  isActive: boolean;
}

interface Product extends BaseEntity {
  categoryId?: string;
  name: string;
  slug: string;
  description?: string;
  shortDescription?: string;
  sku: string;
  price: number;
  originalPrice?: number;
  costPrice?: number;
  stockQuantity: number;
  soldQuantity: number;
  weight?: number;
  volume?: number;
  mainImageUrl?: string;
  imageUrls: string[];
  attributes: Record<string, any>;
  isActive: boolean;
  isRecommended: boolean;
  isHot: boolean;
  isNew: boolean;
  viewCount: number;
  rating: number;
  reviewCount: number;
}

interface ProductSku extends BaseEntity {
  productId: string;
  skuCode: string;
  attributes: Record<string, any>;
  price: number;
  originalPrice?: number;
  stockQuantity: number;
  soldQuantity: number;
  imageUrl?: string;
  isActive: boolean;
}

// 购物车相关
interface ShoppingCart extends BaseEntity {
  userId: string;
  sessionId?: string;
}

interface CartItem extends BaseEntity {
  cartId: string;
  productId: string;
  skuId?: string;
  quantity: number;
  unitPrice: number;
}

// 订单相关
interface Order extends BaseEntity {
  orderNo: string;
  userId: string;
  totalAmount: number;
  discountAmount: number;
  shippingFee: number;
  finalAmount: number;
  status: 'pending' | 'paid' | 'shipped' | 'delivered' | 'completed' | 'cancelled' | 'refunded';
  paymentStatus: 'unpaid' | 'paid' | 'refunded' | 'failed';
  paymentMethod?: string;
  paymentTime?: string;
  shippingAddress: Record<string, any>;
  shippingMethod?: string;
  shippingNo?: string;
  shippingTime?: string;
  deliveredTime?: string;
  buyerRemark?: string;
  sellerRemark?: string;
  cancelledReason?: string;
  cancelledTime?: string;
}

interface OrderItem extends BaseEntity {
  orderId: string;
  productId: string;
  skuId?: string;
  productName: string;
  skuAttributes: Record<string, any>;
  quantity: number;
  unitPrice: number;
  totalPrice: number;
}

// 支付相关
interface Payment extends BaseEntity {
  orderId: string;
  paymentNo: string;
  paymentMethod: string;
  paymentChannel?: string;
  amount: number;
  status: 'pending' | 'success' | 'failed' | 'refunded';
  transactionId?: string;
  payerInfo: Record<string, any>;
  rawResponse: Record<string, any>;
  paidAt?: string;
}

// 请求/响应类型
interface PaginatedResponse<T> {
  items: T[];
  pagination: {
    page: number;
    pageSize: number;
    total: number;
    totalPages: number;
  };
}

interface LoginRequest {
  username: string;
  password: string;
}

interface RegisterRequest {
  username: string;
  email: string;
  password: string;
  phone?: string;
}

interface CreateOrderRequest {
  cartItemIds: string[];
  shippingAddressId: string;
  paymentMethod: string;
  buyerRemark?: string;
}
```

## 3. 数据库表结构设计

### 核心表结构（PostgreSQL）
```sql
-- 已在前面的迁移文件中定义完整表结构
-- 包括：users, user_profiles, user_addresses, categories, products, product_skus,
-- shopping_carts, cart_items, orders, order_items, payments等
```

### 索引策略
- 主键索引：所有表的主键
- 唯一索引：用户名、邮箱、手机号、订单号等
- 复合索引：常用查询组合
- 全文索引：商品名称、描述搜索

## 4. API接口定义

### 认证相关
```
POST   /api/v1/auth/register     # 用户注册
POST   /api/v1/auth/login        # 用户登录
POST   /api/v1/auth/logout       # 用户登出
POST   /api/v1/auth/refresh      # 刷新token
GET    /api/v1/auth/profile      # 获取用户资料
PUT    /api/v1/auth/profile      # 更新用户资料
```

### 用户相关
```
GET    /api/v1/users/addresses           # 获取地址列表
POST   /api/v1/users/addresses           # 添加地址
PUT    /api/v1/users/addresses/{id}      # 更新地址
DELETE /api/v1/users/addresses/{id}      # 删除地址
PUT    /api/v1/users/addresses/{id}/default # 设为默认地址
```

### 商品相关
```
GET    /api/v1/categories                # 分类列表
GET    /api/v1/categories/{id}/products  # 分类商品
GET    /api/v1/products                  # 商品列表（搜索、筛选、排序）
GET    /api/v1/products/{id}             # 商品详情
GET    /api/v1/products/{id}/skus        # 商品SKU列表
GET    /api/v1/products/{id}/reviews     # 商品评价
```

### 购物车相关
```
GET    /api/v1/cart                      # 获取购物车
POST   /api/v1/cart/items                # 添加商品到购物车
PUT    /api/v1/cart/items/{id}           # 更新购物车商品数量
DELETE /api/v1/cart/items/{id}           # 删除购物车商品
DELETE /api/v1/cart                      # 清空购物车
```

### 订单相关
```
GET    /api/v1/orders                    # 订单列表
POST   /api/v1/orders                    # 创建订单
GET    /api/v1/orders/{id}               # 订单详情
PUT    /api/v1/orders/{id}/cancel        # 取消订单
POST   /api/v1/orders/{id}/pay           # 支付订单
```

## 5. 接口测试用例模板

### 用户注册测试
```http
POST /api/v1/auth/register
Content-Type: application/json

{
  "username": "testuser",
  "email": "test@example.com",
  "password": "Password123!",
  "phone": "13800138000"
}

预期响应：
{
  "code": 201,
  "message": "注册成功",
  "data": {
    "user": { ... },
    "token": "jwt-token"
  }
}
```

### 商品列表测试
```http
GET /api/v1/products?page=1&pageSize=20&categoryId=xxx&sortBy=price&sortOrder=asc
Accept: application/json

预期响应：
{
  "code": 200,
  "message": "success",
  "data": {
    "items": [...],
    "pagination": {
      "page": 1,
      "pageSize": 20,
      "total": 100,
      "totalPages": 5
    }
  }
}
```

## 6. 开发团队协作指南

### 前端开发规范
1. **项目结构**: 按功能模块组织代码
2. **组件规范**: 使用TypeScript，定义明确的Props接口
3. **状态管理**: 使用Zustand + React Query
4. **API调用**: 统一使用封装的axios实例
5. **错误处理**: 统一的错误处理中间件

### 后端开发规范
1. **项目结构**: 按领域驱动设计组织代码
2. **代码分层**: Controller → Service → Repository → Model
3. **错误处理**: 统一的错误处理中间件
4. **日志记录**: 结构化日志，包含请求ID
5. **测试覆盖**: 单元测试 + 集成测试

### 接口一致性保证
1. **TypeScript类型共享**: 前后端使用相同的类型定义
2. **API文档**: 使用OpenAPI/Swagger生成文档
3. **Mock服务器**: 前端开发时使用Mock数据
4. **接口测试**: 自动化接口测试保证一致性

### 版本控制
1. **分支策略**: Git Flow
2. **提交规范**: Conventional Commits
3. **代码审查**: 所有代码必须通过PR合并
4. **CI/CD**: 自动化测试和部署

## 7. 开发环境配置

### 前端环境
```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 构建生产版本
npm run build

# 运行测试
npm run test
```

### 后端环境
```bash
# 安装依赖
go mod download

# 启动开发服务器
go run cmd/main.go

# 运行测试
go test ./...

# 代码检查
go vet ./...
go fmt ./...
```

### 数据库迁移
```bash
# 使用Docker启动数据库
docker-compose up -d postgres

# 执行迁移
# 迁移脚本已配置在docker-compose中自动执行
```

## 8. 部署架构

### 开发环境
- 使用Docker Compose一键启动
- 包含所有依赖服务（数据库、缓存、消息队列）
- 支持热重载开发

### 生产环境
- 容器化部署（Docker + Kubernetes）
- 多可用区高可用架构
- 自动扩缩容和监控告警

---

**此文档为前后端开发团队的统一规范，所有开发工作必须遵循此规范。**

*文档版本: 1.0.0*
*更新时间: 2026-02-08 12:58 GMT+8*