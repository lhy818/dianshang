# 电商系统快速部署指南

## 系统要求
- Docker 20.10+
- Docker Compose 2.0+
- 4GB可用内存
- 10GB可用磁盘空间

## 快速开始

### 1. 克隆项目
```bash
git clone <repository-url>
cd ecommerce-project
```

### 2. 启动基础设施
```bash
# 启动数据库、缓存和消息队列
docker-compose up -d postgres redis rabbitmq

# 验证服务状态
docker-compose ps
```

### 3. 启动后端服务
```bash
# 方法1: 使用修复脚本（推荐）
./start_backend.sh

# 方法2: 手动启动
docker run --rm -d \
  --name ecommerce-backend \
  --network ecommerce-project_default \
  -p 8080:8080 \
  -e APP_ENV=development \
  -e DB_HOST=postgres \
  -e DB_PORT=5432 \
  -e REDIS_HOST=redis \
  -e RABBITMQ_HOST=rabbitmq \
  -v $(pwd)/backend:/app \
  -w /app \
  golang:1.21-alpine \
  sh -c "apk add --no-cache git && go mod download && go run cmd/main.go"
```

### 4. 验证后端服务
```bash
# 等待30秒后检查健康状态
sleep 30
curl http://localhost:8080/health

# 检查日志
docker logs ecommerce-backend --tail=20
```

### 5. 启动前端服务（临时方案）
由于前端构建存在问题，可以使用以下临时方案：

```bash
# 进入前端目录
cd frontend

# 安装依赖（如果Node.js已安装）
npm install

# 启动开发服务器
npm run dev

# 或者使用Docker临时方案
docker run --rm -d \
  --name ecommerce-frontend \
  -p 3000:3000 \
  -v $(pwd):/app \
  -w /app \
  node:18-alpine \
  sh -c "npm install && npm run dev"
```

## 环境配置

### 后端环境变量
```bash
# config/config.yaml 中的关键配置
database:
  postgres:
    host: "postgres"      # Docker服务名
    port: 5432
    user: "postgres"
    password: "postgres"  # 与docker-compose.yml一致
    dbname: "ecommerce"
```

### 前端代理配置
前端通过nginx.conf代理API请求到后端：
```nginx
location /api/ {
    proxy_pass http://backend:8080;
}
```

## 服务访问地址

| 服务 | 访问地址 | 用途 |
|------|----------|------|
| 后端API | http://localhost:8080 | RESTful API接口 |
| 前端应用 | http://localhost:3000 | 用户界面 |
| PostgreSQL | localhost:5432 | 数据库管理 |
| Redis | localhost:6379 | 缓存管理 |
| RabbitMQ管理 | http://localhost:15672 | 消息队列管理 |
| RabbitMQ | localhost:5672 | 消息队列服务 |

## 默认凭证

### PostgreSQL
- 用户名: postgres
- 密码: postgres
- 数据库: ecommerce

### RabbitMQ管理界面
- 用户名: admin
- 密码: admin
- 地址: http://localhost:15672

### Redis
- 无密码（开发环境）
- 数据库: 0

## API测试

### 健康检查
```bash
curl http://localhost:8080/health
```

### 用户注册
```bash
curl -X POST http://localhost:8080/api/v1/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "username": "testuser",
    "email": "test@example.com",
    "password": "password123"
  }'
```

### 获取商品列表
```bash
curl http://localhost:8080/api/v1/products
```

## 故障排除

### 常见问题1: 后端启动失败
**症状**: 容器启动后立即退出
**解决方案**:
```bash
# 检查日志
docker logs ecommerce-backend

# 常见原因: 数据库连接失败
# 验证数据库是否运行
docker-compose ps postgres

# 重新启动
docker-compose restart backend
```

### 常见问题2: 前端无法访问API
**症状**: 前端显示网络错误或404
**解决方案**:
```bash
# 检查后端是否运行
curl http://localhost:8080/health

# 检查网络配置
docker network ls
docker network inspect ecommerce-project_default

# 临时解决方案: 直接访问后端
# 修改前端配置指向 http://localhost:8080
```

### 常见问题3: 数据库连接失败
**症状**: 后端日志显示数据库连接错误
**解决方案**:
```bash
# 验证数据库服务
docker-compose exec postgres psql -U postgres -c "\l"

# 检查密码配置
# 确保 config/config.yaml 中的密码与 docker-compose.yml 一致

# 重启数据库
docker-compose restart postgres
```

## 数据初始化

### 查看数据库表
```bash
docker-compose exec postgres psql -U postgres -d ecommerce -c "\dt"
```

### 查看表结构示例
```bash
docker-compose exec postgres psql -U postgres -d ecommerce -c "\d users"
```

## 监控和日志

### 查看所有服务日志
```bash
docker-compose logs -f
```

### 查看特定服务日志
```bash
docker-compose logs -f backend
docker-compose logs -f frontend
docker-compose logs -f postgres
```

### 服务状态监控
```bash
# 实时状态
docker-compose ps

# 资源使用
docker stats
```

## 清理和重置

### 停止所有服务
```bash
docker-compose down
```

### 停止并删除数据
```bash
docker-compose down -v
```

### 清理Docker资源
```bash
# 删除所有停止的容器
docker container prune

# 删除未使用的镜像
docker image prune

# 删除未使用的卷
docker volume prune
```

## 开发工作流

### 1. 开发环境设置
```bash
# 启动开发环境
./start_backend.sh
cd frontend && npm run dev

# 或者使用完整Docker Compose（修复后）
docker-compose up -d
```

### 2. 代码修改和测试
```bash
# 后端代码热重载（已配置）
# 修改backend/目录下的文件会自动重启

# 前端代码热重载
# 修改frontend/目录下的文件会自动刷新
```

### 3. 数据库迁移
```bash
# 自动迁移（开发环境）
# 后端启动时会自动执行数据库迁移

# 手动执行SQL
docker-compose exec postgres psql -U postgres -d ecommerce -f /path/to/sql/file.sql
```

## 生产部署注意事项

### 安全配置
1. **修改默认密码**: 所有服务的默认密码必须修改
2. **启用SSL/TLS**: 数据库和API通信启用加密
3. **防火墙配置**: 只开放必要的端口
4. **环境变量**: 使用环境变量管理敏感信息

### 性能优化
1. **数据库连接池**: 调整连接池大小
2. **Redis缓存**: 配置合理的缓存策略
3. **CDN加速**: 静态资源使用CDN
4. **负载均衡**: 多实例部署

### 监控告警
1. **健康检查**: 实现完整的健康检查端点
2. **日志聚合**: 使用ELK或类似方案
3. **性能监控**: 添加APM工具
4. **告警系统**: 配置关键指标告警

## 支持联系方式

### 问题报告
- GitHub Issues: <repository-url>/issues
- 邮件支持: devops@example.com

### 文档资源
- API文档: http://localhost:8080/swagger (待实现)
- 架构文档: docs/architecture.md
- 部署文档: docs/deployment.md

---
*最后更新: 2026-02-08*
*文档版本: v1.0*